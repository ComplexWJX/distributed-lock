spring:
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test?useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&transformedBitIsBoolean=true&allowMultiQueries=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456


server:
  port: 8014

mybatis:
  mapper-locations: classpath:mapper/**/*Mapper.xml

logging:
  config: classpath:logback.xml
rocketmq:
  name-server: localhost:9876
  producer:
    retry-times-when-send-failed: 2



